pkgname="toru"
pkgver="0.1.0"
pkgrel="1"
pkgdesc="Pacman wrapper with AUR support."
arch=("x86_64")
license=("Apache-2.0")
depends("git", "base-devel", "pacman", "sudo")
makedepends("cargo")
source=("")
sha512sums=("SKIP")

build() {
}

package() {
}